format_version: "11"
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git
project_type: android

workflows:
  primary:
    steps:
      - activate-ssh-key@4:
          run_if: '{{getenv "SSH_CLIENT_KEY" | ne ""}}'
      - git-clone@8: {}
      - install-missing-android-tools@3:
          inputs:
            - gradlew_path: ./gradlew
      - android-build@1:
          inputs:
            - module: app
            - variant: debug
            - build_type: apk
      - deploy-to-bitrise-io@2: {}
